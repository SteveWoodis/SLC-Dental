/**
Find the only item that occurs an even number of times in an array. If there is more that one item that occurs an even number of times, just return one of them. If there are no items that occur an even number of times, return null.

var onlyEven = evenOccurance([1,6,2,4,4,5,6,8,9,6]);
console.log(onlyEven); //4

var onlyEven = evenOccurance([1,1,3,4,1]);
console.log(onlyEven); //null
*/
